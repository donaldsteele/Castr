---
type: synthesis
title: "Castr roadmap and milestone status"
tags: [milestone, decision]
sources: [castr-project-plan]
created: 2026-07-24
updated: 2026-07-25
---

# Castr roadmap and milestone status

**Read this page first when resuming work on Castr after a session restart.** This is the durable, cross-session task list — in-session `TodoWrite` state does not persist, this page does. Update it at the close of every milestone (and whenever milestone status changes) before ending a session or compacting context, per [[castr-project]]'s stated resumability requirement.

---

## 🔻 RESUME HERE — state as of 2026-07-25, written before a planned reboot

### ⚠️ READ THIS BEFORE RUNNING ANY BENCHMARK — the reboot changed the measurement environment

This host had **leaked multicast memberships on Loopback Pseudo-Interface 1** (`References = 0`, no owning socket) for `239.192.55.55` — **Castr's shipped default group** — plus `239.192.57.63` and `239.192.57.64`. While those existed, traffic on the default group **never touched the wire**; it went to loopback.

**Those leaks clear on reboot.** So the next benchmark on the default group will egress via the physical NIC and hit this LAN's hard **~11.8 MB/s multicast ceiling** instead of the ~24 MB/s loopback figures in the run log.

**That will look like a catastrophic regression. It is not.** It is the same code measured over a different network path. Verify with `netsh interface ipv4 show joins` before concluding anything, and check which interface the group resolves to.

Root cause of the ceiling (fully diagnosed, see `docs/benchmarks/throughput-runs.md`): the **link partner** — an eero-class mesh router at `192.168.68.1` — meters multicast to exactly 100 Mbps. Not the NIC, not Windows, not Castr. Evidence: per-frame time linear at 12.4 bytes/µs with **zero fixed intercept**, matches the preamble+IFG-inclusive wire model to 0.07%, broadcast on the same NIC runs 85–90 MB/s, and no stall exceeds 33.6 ms (the max 802.3x PAUSE quantum at 1 Gbps). Unicast on the same NIC hits full line rate (~119 MB/s).

**Implication: further transport optimisation cannot be validated here.** Castr's loopback throughput already *exceeds* this LAN's real multicast ceiling. The prerequisite for any more wire-speed work is a **dumb gigabit switch or a direct cable between two hosts** — cheap, and it is the gating item, not more code.

### Where the code is

- `main` = `a413435`, pushed, in sync with `origin/main`. Working tree clean.
- `bench/m7-instrumentation` = `4ab9891`, pushed. Holds `BenchMetrics` + `tools/bench-m7/` (inert unless `CASTR_BENCH=1`). **Based on pre-M7 `702de7c`** — rebase onto `main` before reusing it to measure current code.
- One worktree survives at `.claude/worktrees/agent-aad9c2ea41236900d` (checked out to the bench branch). `.claude/` is gitignored. Safe to delete with `git worktree remove`.
- **480 tests, 0 warnings** under `-warnaserror`. Docker netem E2E tier green.

### Throughput programme so far

| Milestone | Change | Measured |
|---|---|---|
| M6 | Receiver socket read decoupled into a bounded channel | fixed the 1.6–2.4 MB/s plateau |
| M7 | Repair-storm bounding, PEER_HAVE coalescing, sender echo filter | amplification 2.39× → 1.13×, stalls eliminated; **goodput deliberately unresolved** |
| M8 | Default chunk size 8 KB → 256 KB | 1.33×, and **2.80× under real netem loss** |
| M9 | Merkle-proof space reserved on packet 0 only; datagram budget 1200 → 1472 | **1.41×**; 309 → 184 datagrams per chunk |

Current: **~24 MB/s on loopback** at 100 MiB. All absolutes in the run log are loopback-only — labelled as such.

### ✅ DECIDED 2026-07-26 — the throughput programme is CLOSED, and this is the agreed order of work after it

**Throughput is done, not abandoned.** M6 → M9 took Castr from ~1.6 MB/s to **10.2 MB/s over real Ethernet, which is ~94% of this LAN's hard multicast ceiling** (~11.8 MB/s, imposed by the link partner metering multicast to 100 Mbps — fully diagnosed, see `docs/benchmarks/throughput-runs.md`). Prediction and measurement agree within 6%.

Remaining single-receiver transport work competes for that last ~6% and **cannot be validated on this hardware**, because loopback runs at 2.6× the real ceiling — so any loopback A/B measures a regime that does not exist on the wire. That is the 60,000-byte-datagram mistake with the sign flipped, and this project has paid for it once already. **Do not resume transport optimisation without a different switch or a direct host-to-host cable.**

**Agreed sequence for the next working session, in this order:**

1. **Clear the small backlog.** ~8 genuinely small, unblocked items, mostly one-to-three files each: `SwarmPullSession._chunkCache` (the identical unbounded-cache defect M10 fixed in the multicast tier — now the **only remaining instance in the tree**), offset-keyed fragment reassembly (retires the mixed-budget stranding class from M9 and folds in the tracked M8 assembler rewrite), `SwarmServeListener` connection-task pruning, session-ID uniqueness enforcement in `Castr.Core`, the FIFO-vs-LRU naming correction in `PacketReassembler`/`ChunkPacketAssembler`, and the E2E `tc netem` packet-0 loss concentration. Low risk, no new design decisions, and it leaves the tree clean before anything structural. Closes roughly 8 of the open items below.

2. **Finish fan-out scaling.** This is the product's headline claim — *"one send, hundreds of receivers"* — and the least-validated axis in the codebase: 3 receivers measure **8.01 MB/s against 13.65 at one**, with **5.08× wire amplification** in the worst measured configuration. Includes the `CAROUSEL_STATUS` (tag 16) and `SECTION_REPORT` (tag 17) redesign — see [[proposal-section-based-repair]], **as amended by the architecture review**: use a *cumulative monotone heartbeat, not an edge event*, because an edge event re-creates the "never reached vs. finished" conflation that produced both M7 liveness bugs. Expect 3-5 review rounds; this touches the most defect-dense area in the system.

3. **Set up release automation.** Tag-triggered `release.yml`, self-contained per-RID publishes, checksums plus detached signatures, generated release notes, README install instructions. This was **M5's original scope**, displaced by the showcase work, and is the only real blocker between Castr and being something a person can download and run. `ci.yml`'s existing `package` job already produces the per-platform zips, so this is mostly wiring and signing.

**Why this order** (it is deliberately not the highest-value-first order): the backlog is cheap and makes the tree clean before a structural change lands in the same files; fan-out is the largest genuine engineering item and wants a clean base; release automation is last because there is no point shipping a v1 before the fan-out claim it advertises is validated.

### Next steps, ranked (from a systems-architect + network-engineer design pass)

0. **Get a dumb gigabit switch or direct cable.** Gating item for everything below that claims a real-network number.
1. **Measure the receiver's sustained datagram ceiling.** This number does not exist and several decisions depend on it. Component measurement, not a throughput claim.
2. **Bound `_chunkCache`** — it is unbounded and retains every chunk's ciphertext, so a 60 s transfer at 115 MB/s would retain ~7 GB. This is a **hard wall**, not a rate limit. Cold path can re-read from the `.part` file and re-encrypt: `ContentKey.EncryptChunk` is fully deterministic, so the result is byte-identical.
3. **Receiver per-datagram path**: O(1) `IsComplete` (currently O(ChunkCount) *per datagram*), scatter-into-buffer reassembly (also closes the tracked `new byte[PacketCount][]` pre-sizing item), pooled receive buffers, shardable reassemblers.
4. **Paced sender** (`IPacedSender`: dual token bucket, priority queues, batched drain). **Must land before or with any send batching** — at window=1 the awaited send *is* the pacer, and removing that brake without it is the M6-round-1 failure, which measured −63% goodput and 48.8% receiver-side loss.
5. **`CAROUSEL_STATUS` heartbeat** (new message type, tag 16 — additive and backward-safe). Replaces the `CarouselIdleThreshold` heuristic that caused both M7 liveness bugs.
6. **`SECTION_REPORT`** (tag 17) — section-scoped bitmaps replacing `CHUNK_REQUEST` + `PEER_HAVE`, with suppression-by-overhearing and sender-side union coalescing. **This is the fix for fan-out**, where 3 receivers measured 5.08× amplification.
7. Sender read-ahead pipeline; then rate feedback, last.

See [[proposal-section-based-repair]] for 5–6, amended by the architecture review: use a **cumulative monotone heartbeat, not an edge event** — an edge event re-creates the "never reached vs. finished" conflation that produced both M7 liveness bugs.

### Rules earned the hard way — apply these

- **Changes that reduce datagrams-per-byte are safe to land before the receiver is fixed. Changes that increase datagrams-per-second are not.** Every throughput change that regressed (M6 round 1's send window, window=2, the 60,000-byte datagram experiment) was the second class. M9 was the first thing shipped from the first class.
- **A format change is lockstep only if the receiver's *accept predicate* rejects the new output — go read that predicate**, not the encoder's output shape. Misreading this deferred M9 for two milestones at 1.41×.
- **Record the multicast group with every benchmark run**, and verify which interface it resolves to. Two documented confounders have invalidated whole run sets: the OS page cache (~1.94×) and group-address/interface selection (~1.8×, now root-caused).
- **`MessageCodec` and `ManifestCodec` have opposite extensibility rules.** New *message types* are additive (unknown tags throw and both sessions swallow). Appending a field to the *manifest* is not — `ManifestVerifier.VerifySignature` re-encodes the decoded manifest before verifying, so an old reader rejects the transfer outright.
- **Give mutation/probe work its own worktree.** Two agents editing one worktree produced a stale-binary artifact and a broken build in this session.
- **.NET exposes no datagram batching on Windows** — no `sendmmsg`, no `WSASendMsg`, no RIO; `SendPacketsAsync` destroys datagram boundaries (measured). Linux UDP GSO is reachable via a raw socket option and measured **35× better per datagram**. Near-wire-speed is a Linux-favoured target.

---

## Milestone status

| Milestone | Scope | Status |
|---|---|---|
| M0 — Scaffolding & spikes | Repo init, solution/project skeleton, LICENSE/.gitignore, `/wiki:init`, Ed25519 spike, mDNS/Info.plist spike | **Nearly done.** Repo initialized (git, Apache-2.0 LICENSE, .gitignore). Solution scaffolded: `Castr.Core`, `Castr.Core.Discovery`, `Castr.Cli`, `Castr.Tui` + matching test projects (`Castr.Core.Tests`, `Castr.Core.IntegrationTests`, `Castr.Core.E2ETests`, `Castr.Cli.Tests`) all build and the default scaffolded tests pass. Basic `ci.yml` (build+test matrix across ubuntu/windows/macos) added. `Castr.Gui*` intentionally left unscaffolded — see `src/Castr.Gui/PLACEHOLDER.md` — until M2/M4. `wiki/` initialized and this plan ingested as the first source. **Both M0 spikes complete**: see [[adr-0001-ed25519-library]] (NSec.Cryptography chosen; solution retargeted net8.0 → **net10.0 LTS** as a consequence, .NET 10 SDK installed) and [[adr-0002-mobile-discovery]] (native NsdManager/NWBrowser confirmed usable directly from C#, iOS Info.plist requirements documented). **M0 complete.** Initial commit pushed to `github.com:donaldsteele/Castr` on `main` (SSH auth configured via a dedicated ed25519 key for this machine, added to the user's GitHub account). QA subagent review of the full M0 scaffold found no defects (build/tests, project reference graph, git hygiene, LICENSE, wiki consistency, ADR fact-checks, CI workflow, GUI placeholder honesty — all verified independently, not just read).
| M1 — Castr.Core | Protocol state machines, chunker+Merkle/BLAKE3, manifest sign/verify, trust store+policy, path-safety, transport abstraction (real UDP + in-memory), `IPeerTable`/`RepairCoordinator`, full unit + loopback-multicast integration suite | **Complete, including QA fix.** See [[m1-core-summary]] for full detail. 167 unit tests (`Castr.Core.Tests`) + 4 real-socket integration tests (`Castr.Core.IntegrationTests`), all passing, verified stable across repeated runs. End-to-end `SenderSession`/`ReceiverSession` proven correct: happy path, 5-receiver fan-out from one send, untrusted-sender rejection, tampered-chunk detection + repair recovery, peer-served repair, and real-UDP-socket repair under 15% induced loss. QA subagent review attempted to break path safety / Merkle verification / signature checks (no bypass found) and did find + we fixed one real defect: a `UInt16` length-prefix overflow in `ChunkRequestMessage` encoding for repair batches over 65,535 chunks (realistic for large cold-start bulk repairs), widened to `UInt32`. Documented scope trims: single-pass carousel (no repeating rounds), repair request/response are multicast-broadcast rather than unicast-targeted with no NACK suppression. **Open risk carried to M3**: CI multicast behavior on GitHub-hosted runners is unverified — only confirmed working on this local Windows dev machine so far. |
| M1.5 — Payload encryption retrofit | X25519 keypairs, `JOIN_REQUEST`/`KEY_GRANT` handshake, ChaCha20-Poly1305 chunk encryption, Merkle-over-ciphertext, updated `SenderSession`/`ReceiverSession`, new tests | **Complete, including QA review.** See [[m1.5-encryption-summary]] for full detail. 186 unit tests (`Castr.Core.Tests`, up from 167) + 4 real-socket integration tests (`Castr.Core.IntegrationTests`), all passing. QA subagent independently rebuilt/re-ran the suite and actively verified tamper rejection, cross-context AAD rejection, per-file nonce uniqueness, MITM-proof manifest binding, and that the new handshake cannot bypass TOFU — no defects found. Two deliberate, QA-accepted deviations from the original wording in [[wire-protocol]]: `JOIN_REQUEST`/`KEY_GRANT` travel over the shared multicast channel (not a separate unicast socket), and the content key is generated outside `SenderSession`'s constructor and injected. `Castr.Core` now sends ciphertext-only chunk payloads. |
| M2 — CLI, TUI, Desktop GUI (parallel) | `Castr.Cli`, `Castr.Tui`, `Castr.Gui.Desktop`, each with its own test suite | **Complete, including QA review (PASS-WITH-CONCERNS).** See [[m2-ui-summary]] for full detail. Added a new `Castr.Core` observability contract first (`TransferProgress`/`ProgressChanged` events, `ITrustPrompt` interactive callback closing the previously-inert `TrustOutcome.PromptRequired` path) — QA'd separately before the three UI surfaces were built against it, no defects found. Then built in parallel: `Castr.Tui` (Spectre.Console `TransferDashboard`, library consumed by Cli), `Castr.Gui`/`Castr.Gui.Desktop` (newly scaffolded Avalonia MVVM app, verified via `Avalonia.Headless`), and `Castr.Cli` (System.CommandLine `send`/`receive`/`trust` commands, real loopback transfer personally verified). Full solution: 244 tests passing (Core 191, Cli 25, Tui 16, Gui 7, IntegrationTests 4, E2E 1), 0 warnings. QA's one non-blocking finding — a pre-existing M1 transport gap where large chunk sizes crash the sender and silently hang the receiver over real UDP (the documented chunk/wire-packet split was never implemented) — was mitigated with a `Castr.Cli` fail-fast `--chunk-size` guard rather than blocking M2 on a full Core fix; the underlying fix is tracked below and in [[wire-protocol]]. |
| M3 — Test/CI hardening | Full GitHub Actions matrix, Testcontainers E2E fan-out, security test pass (traversal/tamper/replay), `/wiki:lint` | **Complete, including two rounds of QA.** See [[m3-test-ci-hardening-summary]] for full detail. Closed the M2-carried transport gap (real two-level chunk/wire-packet split, `ChunkPacketizer`/`ChunkPacketAssembler` + `WirePacketizer`/`PacketReassembler`), built a real Testcontainers multi-container E2E fan-out suite (real `tc netem`-induced kernel loss, byte-identical recovery via repair), added 11 security tests (traversal/tamper/trust-store/TOFU-bypass), fixed a long-flaky `Castr.Tui.Tests` timing test, and — the milestone's biggest find — discovered and fixed CI's `macos-latest` leg had been silently failing since M1 (missing `IP_MULTICAST_IF` on macOS), verified green on real GitHub Actions runs via the newly-installed `gh` CLI, plus added a separate Docker-gated `e2e-docker` CI job. QA (dedicated review of the chunk/wire-packet split, then a combined review of the full M3 surface) found and fixed one should-fix defect (an unauthenticated reassembly memory-exhaustion DoS) and one incidental concurrency bug in `ReceiverSession` surfaced by the E2E suite's realistic load; final verdict PASS. 295 tests passing (Core 236, Cli 30, Tui 16, Gui 7, IntegrationTests 6) plus a separate opt-in E2E tier (3 tests), 0 warnings. **Follow-up after M3 closed**: `ci.yml` gained a `package` job (main-branch pushes only, gated on `build-and-test` + `e2e-docker` passing) that self-contained-publishes `Castr.Cli` and `Castr.Gui.Desktop` for five RIDs (win-x64, win-arm64, osx-x64, osx-arm64, linux-x64) from a single `ubuntu-latest` runner and uploads each as a downloadable zip workflow artifact — verified for real via a throwaway PR before merging to `main`. This is a CI convenience, not a release: builds are unsigned/unnotarized and there's no versioning/checksums/GitHub Release yet — that remains M5's scope (see below). |
| M4 — Mobile | `Castr.Core.Discovery` native mDNS impls, unicast swarm client, `Castr.Gui.Android`/`Castr.Gui.iOS`, sideload packaging | **Complete, including QA review (PASS-WITH-CONCERNS).** See [[m4-mobile-summary]] for full detail. New TCP unicast "swarm pull" tier in `Castr.Core` (`SwarmPullSession`/`SwarmServeListener`, length-prefixed framing, shared `ManifestAdmission` trust gate) alongside the existing multicast tier; real native mDNS (`NsdManager`/`NWBrowser`) in `Castr.Core.Discovery`; `Castr.Gui.Android` (real debug-signed APK, CI-verified) and `Castr.Gui.iOS` (real Xcode-linked `Castr.Core.Discovery` head, CI-verified) GUI heads. A real security defect was found and fixed *during* M4 in the core M1-era multicast path too, not just the new mobile code: `MerkleProof.Verify` didn't authenticate its own `LeafIndex` field against the proof's `Steps`, letting a malicious relaying peer relabel a genuine chunk's wire position and permanently stall a receiver — fixed by deriving `LeafIndex` from `Steps` inside the primitive itself. 359 tests passing, 0 warnings. QA found no security/trust/verification defects and confirmed the test/build numbers independently; two non-blocking findings (unbounded connection-task accumulation in `SwarmServeListener`, iOS view-model missing pull-cancellation) plus the already-known view-model duplication and iOS Simulator link gap are carried forward below. |
| M5 — Showcase docs | Real, hands-on-captured screenshots/GIFs of `Castr.Cli`, `Castr.Tui`, `Castr.Gui.Desktop` doing real loopback transfers, framed around concrete use cases, linked from the README | **Complete.** `docs/SHOWCASE.md` — LAN-party (desktop GUI), sysadmin-fleet-push (CLI), test-lab-fanout (TUI) framings, each with real captured media, linked from a new README "See it in action" section. Captured via `ffmpeg`'s `ddagrab` (DXGI Desktop Duplication) — `gdigrab` was tried first and rejected: it desaturates Windows Terminal's GPU-composited rendering. Capture tooling generalized and committed at `tools/demo-capture/` (`New-DemoFiles.ps1`, `capture-cli/tui/gui.ps1`, `ConvertTo-Gif.ps1`, `WinPos.ps1`) so the media can be regenerated later — see [[m6-throughput-pipelining]] below, since a regeneration at the corrected throughput is queued up next. Two known, deliberate gaps, not oversights: mobile (Android/iOS) has no showcase media (no emulator/Mac on the dev machine — tracked alongside the existing mobile hands-on-interop open item below), and formal release automation (tag-triggered `release.yml`, checksums/signatures, GitHub Release notes — the *original* M5 scope from [[castr-project-plan]] before this session's showcase work took the M5 slot) has not been started; it's demoted to an explicit open item below rather than silently dropped. |
| M7 — Repair amplification / gossip / own-echo | Bound the self-inflicted repair storm (P0), make `PEER_HAVE` gossip linear rather than quadratic (P1), and stop the sender decoding its own multicast echo (P2) | **Implemented, reviewed twice, NOT merged.** See [[m7-repair-amplification]]. Round 1: QA **FAIL** on a reproduced liveness hang plus systems-design **MERGE-WITH-CHANGES**; round 2 fixed the hang (per-file carousel-idle valve keyed on watermark advancement from carousel deliveries only), added `RepairOptions.MaxRequestsPerPass` as the real amplification bound, replaced an inert pass-counting jitter with per-chunk wall-clock jitter + a wall-clock-clamped backoff, capped the receiver's own relay path, and made `PeerHaveInterval`/`CarouselIdleThreshold` injectable. Defensible outcomes: **wire amplification 2.39× → 1.13×**, duplicate chunk transmissions **−99.7%**, PEER_HAVE fragment gossip **20,557 → 68 datagrams**, the **periodic ~468 ms/695 ms stall pattern eliminated**, and two liveness bugs fixed. The implementer's **"+112.6% goodput" claim was withdrawn** as degraded-host recovery; net goodput effect is unresolved pending a warm/interleaved n≥3 A/B on a host reproducing the campaign's 13.65 MB/s baseline, ideally by an independent party. 431 tests, 0 warnings, `ChaosTransport` tests unmodified. |
| M6 — Send-path throughput/pipelining | Root-cause the ~1.6-2.4 MB/s demo-GIF plateau; fix it; re-benchmark; re-capture M5's showcase media at the new throughput | **Complete, including two independent QA + systems-design review rounds.** See [[m6-throughput-pipelining]] for full round-by-round detail. Three-round investigation: round 1 pipelined `SenderSession`'s send loop with a bounded-concurrency window but shipped an unvalidated default (`sendWindowSize=2`) that both reviewers rejected after re-measuring it as a consistent regression, not the "roughly neutral" result round 1 reported. Round 2 root-caused the *real* bottleneck as receiver-side — `ReceiverSession`'s per-packet chain (verify/decrypt/**disk write**/outbound broadcast) was fully serialized under one lock, and `UdpMulticastTransport.ReceiveAsync` had no decoupling from it, so the OS receive buffer only drained as fast as that whole chain ran — and fixed it there: a dedicated reader task now drains the socket into a bounded `Channel<ReceivedPacket>`, plus explicit `SO_RCVBUF`/`SO_SNDBUF` sizing (inspired by user-sourced research into `uftp-multicast`, a near-wire-speed reference implementation that treats explicit socket-buffer sizing as a primary throughput lever). With that fix in place, `sendWindowSize=2` became a *consistent* ~1.4-1.6x win with no collapse, confirmed on both a 1:1 transfer and a 3-receiver fan-out benchmark with one deliberately throttled receiver — but the shipped default stayed at **1** (a true no-op vs. pre-M6 behavior) pending outside validation, with `castr send --send-window-size` added as an opt-in for anyone who validates a higher window on their own hardware. Round 3 (final sign-off): both reviewers independently confirmed round 2's fix and data, systems-design recommended merge outright, QA found one last real regression (`UdpMulticastTransport.DisposeAsync` no longer idempotent) which got fixed along with hardening exception propagation in the new reader loop and adding a code-level guardrail comment on the one known, deliberately-unfixed correctness gap (carousel+repair can transiently double real concurrency — measured, documented, and judged not worth a ~30% throughput cost to close at the current default). 367 tests passing (up from 359 pre-M6), 0 warnings, real-socket `ChaosTransport` tests unmodified and green throughout. Merged to `main`. |

## Definition of done (every milestone)

Per [[castr-project-plan]]: tests passing, a `/wiki:ingest` summary committed, `graphify --update` run if code changed, and a QA subagent pass (an independent `Agent` spawned to exercise/review the milestone's tests and behavior) before moving to the next milestone.

## Open items carried forward

- **🔵 PROPOSAL (not scheduled, no code): section-based repair gating** — see [[proposal-section-based-repair]]. Replace the `CarouselIdleThreshold` heuristic with an explicit sender-emitted section-completion message, so a receiver *learns* a chunk has been transmitted rather than inferring it from a timeout. **Explicitly not a throughput change**: amplification is already 1.05× against a 1.00× floor, the measured bottleneck is per-datagram cost, and the M7 campaign measured deferring repair in isolation at **−11%**. The motivation is deleting the bug class behind both M7 liveness defects (the "never reached vs. finished are indistinguishable at seed time" conflation), removing an arbitrary constant both reviewers flagged, and restoring the independent amplification bound M8 regressed. Gate on **sections, not the end of the transfer** — the whole-transfer variant reintroduces the burst-then-stall shape this whole workstream started from. Blocked on nothing now that M8 has merged; should be **designed and reviewed before implementation**, since both prior defects here were design errors that survived code review.

- **🔴 ACTIVE: M6 improved average throughput but not consistency — a bursty burst/stall delivery pattern remains, and the investigation has identified a self-inflicted congestion loop** (new, 2026-07-25, post-M6 user field report: ~8 MB/s but visibly bursting then stalling). Two independent agents (systems-design + a UFTP reference contrast) converged on the same primary cause, then a **91-run instrumented measurement campaign** measured it directly. Full write-up in `docs/METHODOLOGY.md`; all numbers in `docs/benchmarks/throughput-runs.md`.
  - **STATUS: P0, P1 and P2 are implemented in an isolated worktree but NOT MERGED** — see [[m7-repair-amplification]]. Round 1 of review returned **QA FAIL** (a reproduced liveness hang: the carousel-idle valve used one global timer refreshed by any chunk-bearing datagram, and since repair responses are multicast, any peer's repair traffic — or a replay of an already-held chunk — held every receiver's valve shut forever) plus **systems-design MERGE-WITH-CHANGES**. Round 2 fixed the hang (valve now keyed on that file's watermark advancing from a carousel delivery, per file), added the per-pass request cap that actually bounds amplification, and replaced an inert pass-counting jitter with per-chunk wall-clock jitter. **Retained claims: wire amplification 2.39× → 1.13×, duplicate chunk transmissions −99.7%, PEER_HAVE fragment gossip 20,557 → 68 datagrams, and the periodic stall eliminated. The "+112.6% goodput" claim was WITHDRAWN** — it was recovery from a degraded host state (that run set's own baseline was 2.8× below the documented warm baseline). **Net goodput effect remains unresolved** and the campaign's −11% warning below is *not* refuted. P3–P6 remain unimplemented.
  - **⚠️ MEASURED RE-RANKING — the derived priority order below was wrong.** The boring parameter change beats the elegant protocol fixes: **chunk 256K alone is +77%**, and **chunk 256K + datagram 60000 reaches 0.81 s / 98.6 MB/s (7.2× baseline) at *lower* wire amplification (1.82×) than today's 2.52×**. So **P5 and P4 are now the top-ranked work, not P0/P1.** Retain P0/P1/P2 for correctness and scaling reasons, but expect the throughput to come from chunk/datagram sizing.
    - **UPDATE (2026-07-25, M8): P5 shipped and delivered 1.33×, not +77%** — because P0 (M7) landed first and had already claimed the storm-collapse half of that number. The two changes were partially claiming the same win, which is exactly what "these multiply rather than add" should have predicted. **P4 (datagram budget) is now the top-ranked remaining single change** and is untouched at 1200; the campaign's 7.2× figure pairs it with the chunk size, so it should be re-measured against the post-M7, post-M8 baseline rather than assumed.
    - **UPDATE (2026-07-25, M9 stage 1): P4 shipped and delivered 1.42×** against the post-M7/post-M8 baseline — and the campaign's 7.2× pairing is now formally **withdrawn**, because it depended on 60,000-byte datagrams that only loopback can carry unfragmented. Re-measuring rather than assuming was right for the third time running.
  - **⚠️ P0 cannot be fixed in isolation.** Disabling premature repair *alone* measured **11% slower** (6.60 s vs 5.86 s; the carousel itself slowed 4.76 → 6.60 s), because `HandleChunkRequestAsync` runs concurrently with the carousel and the redundant repair stream is currently **the only thing giving the sender send-path parallelism**. The waste is accidentally load-bearing — the same trap M6 round 1 hit from the other direction. Pair any amplification fix with real send batching.
  - **Measured facts that revise the analysis below:** the stall period is **5.10 s** (`RepairOptions.RequestTimeout`), not 250 ms — only 4 of ~65 repair passes ever emit a request. Amplitude is **0.00 → 39 MB/s**, with up to **70% of a transfer in total dead air** at window 4. The first repair request asks for **10,212 of 10,240 chunks** while the carousel is ~1% done, so **the file is sent twice** (2.52× amplification, zero actual loss). **Both sides are bound by per-datagram cost (~32 µs each way)**, not bytes or crypto — `SendToAsync` is 66% of sender CPU; GC is not a factor. **The sender drops 87–97.5% of datagrams offered to its own socket**, so receivers' `CHUNK_REQUEST`/`JOIN_REQUEST` are dropped at the sender with **>90% probability during the carousel** (P2 is a correctness hazard, not just waste). **`SO_RCVBUF` is granted in full — no clamping.** **Receiver-side loss is entirely self-inflicted:** 0% with repair off, 12% at baseline, 48.8% at window 4.
  - **Methodology finding that invalidates part of M6's data:** the **OS page cache is a ~2× confounder** (12.6 s cold vs 6.5 s warm on identical config and identical datagram counts). **M6 round 2's "consistent 1.4–1.6× win at window 2" did not reproduce** — warm/interleaved/n=3 gives window 2 = **−13%** and window 4 = **−63%**. Keeping `DefaultSendWindowSize = 1` is vindicated, and the open item below about raising it should be considered **closed against raising it** pending genuinely new cross-machine evidence. Also `netstat -s -p udp` "Receive Errors" reads **0** despite 200k–370k dropped datagrams — unusable on Windows.
  - **Pre-existing test flaw — FIXED in the M7 worktree:** `UdpMulticastTransportTests` bound **hardcoded UDP ports** and failed with `WSAEACCES` when a system process held one (root cause confirmed: `DnsService` holds UDP 46101 on this host). Now asks the OS for a bindable ephemeral port via a `FreeUdpPort()` helper; the tests still bind real sockets and assert real delivery.
  - **New wire-format fact worth keeping (established during M7 review):** a **new message type is additively backward-compatible** — an unknown tag throws in `MessageCodec.Decode` and both sessions already swallow that — but **appending a field to an existing message type is NOT**: a new reader hits end-of-span on an old message and throws. So the eventual `ChunkRequestMessage` target field (needed for repair-response deduplication) must be a new type or a format-version bump, not an append.
  - Derived ranking as originally recorded, retained for the mechanisms (magnitudes now superseded above; P0/P1/P2 implementation status is in [[m7-repair-amplification]]):
  - **P0 — repair storm (no wire change needed).** `ReceiveRunner.RunRepairLoopAsync` polls every 250 ms unconditionally; at the first tick `ReceiverSession.RequestRepairsAsync` lists *every* not-yet-arrived chunk (~9,900 of 10,240 for an 80 MiB transfer) with no cap and no notion of what the carousel has actually sent, `RepairCoordinator.PlanRepairs` puts them all in one ~35-fragment all-or-nothing request, and `SenderSession.HandleChunkRequestAsync` re-reads/re-encrypts/re-proofs and re-sends all of them **concurrently with the still-running carousel**. Roughly doubles wire bytes and sender CPU. Worse, `_pending` is marked *before* the request is sent and `RepairOptions.RequestTimeout` is 5 s, so losing one request fragment (likely — it's injected into the congestion it's trying to fix) costs a full 5 s of silence. **The real stall quantum is ~5 s, not 250 ms.** Fix: cap requests to one datagram, add a carousel watermark, mark `_pending` after a successful send, add the jitter/backoff that [[repair-protocol]] already claims, and get `HandleChunkRequestAsync` off the sender's incoming loop.
  - **P1 — `PEER_HAVE` is quadratic in file size (no wire change needed).** `ReceiverSession.HandleChunkAsync` broadcasts the *entire* bitmap on every verified chunk, as an awaited socket send **inside `_stateGate`**. Total gossip = `FileSize² / (8 · chunkSize²)`: ~13.6 MB for 80 MiB at 8 KB chunks (15% of all datagrams), **~1.31 GB for an 800 MiB file — more than the payload.** Fix: coalesce to a section/time boundary and snapshot-then-send outside the gate. Peer discovery (the dual purpose noted in [[wire-protocol]]) is fully preserved. Note this cost was already visible in M6's own chunk-8192→60000 measurement and was misattributed to datagram count at the time.
  - **P2 — the sender receives and decodes its own entire transmission (~5-line fix).** `UdpMulticastTransport` always `AddMembership` + `MulticastLoopback=true` for both roles, and `SenderSession` has no own-echo filter (unlike `ReceiverSession`, which filters its own `PEER_HAVE` and its own `CHUNK_REQUEST`). All ~133,120 of its own chunk datagrams loop back, get fully `MessageCodec.Decode`d, and are dropped — starving its own 4 MB buffer and 4096-slot inbox so the kernel drops **real `CHUNK_REQUEST`/`JOIN_REQUEST` control traffic.** Fix: pre-decode message-type filter on `datagram[1]`.
  - **P3 — `_stateGate` is taken per *datagram* (13× per chunk), holding a disk write and two socket sends.** ~30,720 serialized awaited I/O ops per transfer. Crypto is not the cost (~8 µs/chunk); the I/O placement is.
  - **✅ P4 — DONE (2026-07-25, M9 stage 1): proof space is reserved on packet 0 only, and the datagram budget is MTU-derived (1200 → 1472) and exposed as `--datagram-size`.** See [[m9-datagram-efficiency]] and the 2026-07-25 M9 section of `docs/benchmarks/throughput-runs.md`. **309 → 184 datagrams per 256 KiB chunk (1.68×), 846 → 1,425 bytes of payload in the median datagram (+68%), 1.42× goodput (5.80 s → 4.07 s, n≥17, warm, interleaved, 52/52 byte-identical), wire amplification 1.052× → 1.031×.** The prediction was exact to the datagram *and* to the byte in all three arms.
    - **The "requires sender+receiver lockstep" classification above was wrong**, and the correction is the durable part: `ChunkPacketAssembler.TryAssemble` sums each fragment's *actual* length against `CiphertextLength` and never assumed uniform fragments, so an unmodified receiver reassembles variable-length fragments correctly (tested). The real consequence is that `Offer` rejects metadata disagreeing with the first packet seen for a chunk, so a chunk relayed by an **old-slicing peer is dropped per chunk** — that peer stops helping repair, the receiver converges from a source that agrees. It **degrades, never corrupts**, and it is a pre-existing property of mixed datagram budgets rather than something this change introduced.
    - **Frame the win as datagram count, not bandwidth, and not as a syscall ceiling.** Wire efficiency moves only 92.2% → 95.1% while datagram count falls 40%. The per-datagram cost was measured directly and is **not** syscall time: 4.7 µs unjoined, 32.1 µs with one receiver joined, **7.3 µs on a real NIC** — the loopback figure is the kernel's inline multicast fan-out copy. The "~37 MB/s syscall ceiling" framing is withdrawn. **Always quote the datagram size beside a loopback number**: the campaign's 98.6 MB/s needed 60,000-byte datagrams, which a real 1500-MTU segment shatters into ~41 IP fragments.
    - **MTU auto-derivation was implemented and then removed in review; `--datagram-size` is explicit-only.** Deriving from the interface MTU is sound in isolation (TTL=1 ⇒ no routers ⇒ path MTU *is* the interface MTU) but it **manufactures a peer mismatch nobody decided on** — a laptop on a 1500-MTU LAN and a peer behind a 1400-MTU VPN pick different budgets automatically, and mismatched budgets silently lose peer repair relay. The flag must match on every peer in a transfer; it is documented as such and contributes none of the measured win. **A DontFragment probe must never be used**: on Windows, `Socket.DontFragment` on a *multicast* socket is silently ignored — it reads back `true` and fragments anyway up to 65,507 bytes, so a DF probe would report that 60,000 "fits".
    - **QA found and this fixes a *stranded chunk*, not merely a degraded one.** `Offer` pinned a chunk's buffer to whichever slicing arrived first, and `Forget()` only runs after a *successful* assembly — so a mismatched peer's partial relay made the chunk uncompletable even by a full, correct re-delivery, with no recovery path but LRU eviction (needs 64 other pending chunks; a lossless transfer keeps 1–2). Fixed by dropping the **stale partial** instead of the incoming packet. **No version skew needed to hit it** — two same-version peers at `--datagram-size 1372` vs 1472 reproduce it, which is independently why the auto-derivation above had to go. Note also: a budget mismatch is **not caught** anywhere — direct sender→receiver delivery works fine and only peer relay breaks, silently.
    - **A crash the knob made reachable, fixed:** `Split` could throw *mid-carousel* when budget, chunk size and file size were jointly impossible (`--datagram-size 548` + `--chunk-size 8192` on 1 GB ⇒ depth-17 tree ⇒ 571-byte proof ⇒ `548 − 43 − 571 < 1`). Validated in `TransferPreparation.PrepareFileAsync`, where all three terms are known, naming both options in the error.
    - Two arithmetic corrections fell out: `ChunkPacketizer.FixedEnvelopeOverhead` is **43, not 47** (its own comment summed its own fields wrong), and M8's recorded 124,218 `CHUNK_PACKET` count reconciles with 309 datagrams/chunk plus exactly two repaired chunks, not with 310.
  - **🟡 FOLLOW-UP from P4: carry the sender's datagram budget in the manifest.** Today both sides use their own local budget and `ChunkPacketAssembler`'s 8× `minFragmentBytes` tolerance makes the mismatch survivable rather than *correct*. Putting it in the manifest would make the budget a wire parameter and enforce session-constancy by construction. **It is not additively backward-compatible** — verified, not assumed: `ManifestVerifier.VerifySignature` re-encodes the decoded manifest and verifies the Ed25519 signature over those bytes, so an old reader that skipped an appended field fails verification (a *rejected* transfer, not a degraded one), and `ManifestCodec.Decode` hard-rejects any `FormatVersion ≠ 1`. So this needs a manifest format-version bump and genuine sender+receiver lockstep, and was deliberately kept out of the narrow, attributable M9 stage.
  - **✅ P5 — DONE (2026-07-25, M8): `CastrPaths.DefaultChunkSize` raised 8192 → 262144**, matching `ChunkLayout.DefaultChunkSize` and the documented 256 KB–1 MB range it previously contradicted. `SendViewModel._chunkSize` follows, and the GUI's `NumericUpDown Maximum` went 60000 → 1048576 (it would otherwise have silently clamped the new default back down — the one real bug this change surfaced). Sequenced after P0 as required, so the gain is attributable and coarser repair granularity was exercised against a healthy repair path. Full numbers in the 2026-07-25 M8 section of `docs/benchmarks/throughput-runs.md`.
    - **Measured 1.33× (not the +77% the campaign recorded), and both are correct.** The campaign's +77% was pre-M7, where a bigger chunk *also* collapsed the premature-repair storm; M7 has since claimed that half, so amplification is already 1.12× at 8192 and only the permanent part (fewer datagrams, amortised proof, smaller gossip term) is left. Expect ~1.33× on post-M7 code; a result near 1.77× would be evidence M7 regressed. **The primary justification is consistency, not throughput:** `ChunkLayout.DefaultChunkSize` was *already* 262144 and wire-protocol.md already specified the 256 KB–1 MB range, so this is "make the CLI and GUI match what Core shipped", with the measurement as supporting evidence.
    - **Under real kernel `tc netem` loss the change is a 2.80× *improvement*, not a regression** (32 MB, 10% loss, interleaved n=6, 12/12 byte-identical) — fewer, larger repair round trips beat many small ones when each round trip is quantised by the 5 s `RequestTimeout`. Read 2.80× as an upper bound: the loss filter also drops some of the 8 KiB arm's larger `CHUNK_REQUEST` datagrams.
    - **Docker E2E green on the new default** — 7/7, 5/5 (20% netem), 9/9 (10% netem) receivers all byte-identical, thousands of real kernel drops, tests unmodified.
    - **1 MiB was measured and deliberately rejected**, on three independent grounds: only +7.7% over 256 KiB while quadrupling repair granularity, roughly half the carousel-idle headroom (10.4× vs 36×), and it **timed out entirely** under deliberate CPU contention where 256 KiB completed.
    - **False-idle exposure under contention is pre-existing and this change *reduces* it** — at CPU saturation the old 8 KiB default produced **more** false idles than 256 KiB (7 vs 3), and every false-idled transfer still completed byte-identical (the failure mode is amplification and latency, never correctness). Note the 36× idle margin is a property of an unloaded host, not a safety factor: a harsher in-process harness measures ~2.7× at *both* chunk sizes, and on a contended host the threshold is breached at any chunk size.
    - **Known, deliberate surface disagreement:** the GUI caps chunk size at 1 MiB (top of the documented range) while the CLI allows 16 MiB (`CastrPaths.MaxChunkSize`). The GUI is a spinner with no room to explain consequences; the CLI is the escape hatch for someone who has measured their own hardware. Documented in both places rather than left to be discovered as an inconsistency.
  - **⚠️ M8 REGRESSED ONE OF M7'S OWN INVARIANTS — disclosed, accepted, worth watching.** `RepairOptions.MaxRequestsPerPass` exists because round 2 of M7 review found that any false-idle in the carousel valve restored the full repair storm, self-reinforcingly, and the cap was added so amplification had a bound that *did not depend on the watermark being right*. At 256 KiB that is no longer true for ordinary files: `4 × 268 = 1,072` chunks per pass exceeds the entire chunk count of any file below **268 MB**, so for such a file **the carousel watermark is once again the only thing standing between the system and a full-file repair storm** — precisely the blocking condition the cap was introduced to remove. The trade is *reduced probability, increased consequence*: the idle margin is 36× with zero false-idle events in 16 instrumented runs, but the per-pass blast radius went from ~8.8 MB to a whole file. Accepted because the byte-denominated fix below is the real repair, not a smaller count.
  - **🔴 PROMOTED TO THE NEXT MILESTONE (2026-07-25, M9 review): a byte-denominated serve cap on the sender.** `MaxChunksPerRequest`'s blast radius has now silently rescaled **twice, monotonically worse — 2.2 MB → 67 MB → 84 MB of data commandable by one request datagram** (chunk size 8 KiB → 256 KiB in M8, then datagram budget 1200 → 1472 in M9). The structural reason is that the constant is *derived from the datagram budget* while its meaning is *bytes served*, and chunk size decouples the two, so it will keep rescaling on any future change to either knob. The fix is one sender-side place: cap `SenderSession.HandleChunkRequestAsync` by **bytes served per request** (~8 MB) rather than index count, which bounds it regardless of both knobs. Deliberately not done in M9 — it changes sender behaviour under load, where M9 was scoped to be attributable. Unpinning 268 → 336 was right; pinning a count would have made the constant assert a falsehood.
  - **🟡 FOLLOW-UP (one item, not two): denominate `MaxRequestsPerPass` and `MaxChunksPerRequest` in bytes rather than counts.** Both are count-denominated quantities whose real cost is bytes, which is the single root cause of the entry above *and* of the fact that one 1,200-byte request datagram now commands 67 MB of multicast — **~55,000× byte amplification**. Expressing both in bytes makes them chunk-size-independent **by construction**, so this whole re-validation exercise never has to be repeated on the next chunk-size change. Do them together; fixing either alone leaves the other as the binding count-denominated term.
  - **🟡 NEW, found while re-validating M7's constants at the larger chunk (2026-07-25, M8) — two hardening gaps.** Both are pre-existing. **The first was partly fixed in M8 after review**; the rest is deferred.
    - **`ChunkPacketAssembler` pre-sizes `new byte[PacketCount][]` from a wire-supplied count. ✅ Bound tightened in M8; ⏳ the data-structure fix is still deferred.** The severity was initially mis-scoped as an M8 problem: **the ceiling is manifest-derived, so it is set by `CastrPaths.MaxChunkSize = 16 MiB`, not by the default** — a sender legitimately choosing `--chunk-size 16777216` already produced `new byte[16777232][]` = ~134 MB from one crafted datagram, i.e. this was reachable on `main` before M8 and M8 only raised the *typical* exposure. Two things landed: a third validation clause bounding `PacketCount <= ceil(CiphertextLength / minFragmentBytes) + 1`, with `minFragmentBytes` plumbed from the session's own datagram budget at a deliberate 8× tolerance (≤1,749 packets admitted against a legitimate ~310 — 5.6× headroom for correct senders, ~150× off the attack), and `DefaultMaxPendingChunks` 1024 → 64 (1024 was an 8 KiB-regime artifact; it is ~16× more partial chunks than the 4096-slot inbox can hold in flight). Together **2.1 GB → ~0.9 MB**. Interop risk is near zero because `Offer` already rejects a `PacketCount` disagreeing with the first seen for that chunk. **Still deferred:** stop pre-sizing from the claimed count at all, holding fragments in a dictionary keyed by packet index so memory tracks fragments *received* rather than *claimed*. That is a data-structure change on a hot path and wants its own review; tightening an existing bound did not. **M9 adds a second, independent reason to do this rewrite and one change to its design: key fragments by *byte offset* rather than packet index.** Packet-index keying is what makes two slicings of the same ciphertext mutually unusable — the root of M9's stranded-chunk defect and the reason a datagram budget cannot be chosen per peer. Byte-offset keying makes slicings interchangeable and retires the class; it also subsumes the "must match on every peer" contract `--datagram-size` currently carries by documentation alone. Note the M8 figures in this bullet are pre-M9 (a legitimate 256 KiB split is now ~184 packets, admitted against ≤1,426). Note the severity class: ~1024 datagrams (~1.2 MB) forcing GBs of long-lived LOH is **~1800× asymmetric and terminates the process**, unlike a flood, which degrades only while it lasts.
    - **`ManifestFileEntry.ChunkSize` is never range-checked** — not in `ManifestCodec.Decode`, not at admission. It is covered by the sender's signature, so this needs a *trusted* sender and is robustness rather than a remote hole; but a value near `int.MaxValue` overflows `CiphertextBoundForChunkSize` negative and throws `ArgumentOutOfRangeException` straight out of the receive loop (only `MessageCodec.Decode` is wrapped), and any large-but-not-overflowing value re-opens the allocation ceiling that line exists to close. Fix: range check at manifest admission — a change to which manifests are *accepted*, so it wants review.
  - **✅ ROOT-CAUSED (2026-07-25, M9): the multicast group-address confounder is leaked loopback memberships — and the "not interface selection" conclusion was exactly backwards.** `netsh interface ipv4 show joins` shows stale `References = 0` memberships on `Loopback Pseudo-Interface 1` for exactly the "fast" groups (`239.192.55.55`, `239.192.57.63`); every "capped" group has a membership only on Ethernet. Causal: joining `239.192.57.64` on loopback flipped it **11.90 → 329 MB/s** while control `.65` stayed at 11.88. **The two classes were two different network paths, not two behaviours of one.** The M8 bullet that "ruled out" interface selection tested `239.192.55.55` — the one group already resolving to loopback — so it was structurally incapable of showing a difference; it is **withdrawn**, and the campaign's **"loopback is 3.1× faster" finding is un-retracted**. Consequences: **every benchmark in this repo taken on the shipped default group (M6–M9) is a loopback number** — ratios survive, absolute MB/s do not describe a LAN; the three leaked joins clear **only on reboot**; and the rule becomes **record the interface a group actually resolves to, not just the group**. Also record the benchmarking trap found alongside it: with the 4 MB `SO_SNDBUF`, the first ~4–5 MB of a wire transfer drains at 119–133 MB/s (~30 ms) before the cap engages, so **any transfer under ~4 MB measures buffer-fill speed, not wire speed**.
  - **✅ ROOT-CAUSED (2026-07-25, M9): the real-NIC ~12 MB/s cap is the link partner, and one systems-design prediction is refuted.** Not the sender's inline `SendChunkAsync` read/encrypt/proof bubble: per-frame time is linear at 12.4 bytes/µs with a **zero fixed intercept**, matches the preamble+IFG-inclusive wire model to 0.07%, and **broadcast on the same NIC runs at 85–90 MB/s** while only `01:00:5e:*` destinations are capped — an eero-class mesh router metering multicast to exactly 100 Mbps. Not the NIC, not Windows, not Castr. The inline bubble is still worth tracking as a **future** constraint (~300–500 µs per chunk, ~4% duty cycle today, becomes binding as per-datagram cost falls), not as a contributor to this cap.
  - **🟡 NEW E2E environment traps (2026-07-25, M8), both cost ~40 minutes:** the fixture reuses a cached `castr-e2e-tests:latest` and will silently validate *stale* code (`docker rmi -f` it before any run whose result depends on a code change); and Testcontainers hangs indefinitely against a Docker Desktop `desktop-linux` context, unfixable via `DOCKER_HOST` because the docker CLI and Docker.DotNet require different `npipe` slash counts and Castr's skip-gate shells out to the CLI. Fix is `~/.testcontainers.properties` with `docker.host=npipe://./pipe/dockerDesktopLinuxEngine`.
  - **P6 — `_chunkCache` never evicts** (writes at `ReceiverSession.cs:352`, no `Remove` anywhere) — ~100 MB live set plus ~143k small objects for an 80 MiB transfer, guaranteeing gen2 GC pauses *during* the transfer. An independent contributor to rate inconsistency. M6 round 3 flagged the bounded channel as a memory surface and missed this, which is ~25× larger and unbounded in transfer size.
  - **Rejected, with reasons:** porting UFTP's TFMCC congestion control (UFTP's default rate is 1000 Kbps — *slower* than Castr; its wire-speed mode is unpaced exactly like Castr, and its own `cc_type` default is `none`, so this is not the source of its advantage), and adopting UFTP's group-HMAC data-path integrity (Castr's Merkle-over-ciphertext is what makes untrusted-peer repair safe — it is the load-bearing primitive for the whole swarm and mobile tier, and the per-chunk cost should be amortized with bigger chunks, not traded away).
  - **Also open, non-throughput:** `Castr.Tui.TransferDashboard`'s `new SemaphoreSlim(0)` has no max count, so its `SemaphoreFullException` catch is dead code and permits accumulate one-per-progress-event — the render loop never actually waits and re-renders unthrottled. TUI/GUI only (the CLI path is percent-bucketed). One-line fix: `new SemaphoreSlim(0, 1)`.
- **🟡 TRACKED FOLLOW-UP: run the `{P0, P0+w2, P0+w4}` send-window matrix on a host that reproduces the 13.65 MB/s baseline.** Owned outcome of the M7 review (see [[m7-repair-amplification]]). **Why it matters:** the 91-run campaign measured that removing premature repair costs **−11%**, because `HandleChunkRequestAsync` running concurrently with `RunChunkCarouselAsync` is currently the *only* thing giving the sender send-path parallelism — and M7 removes exactly that traffic (duplicate chunk datagrams −99.7%). M7's own A/B could not settle whether that regression materialises, because it ran on a degraded host whose baseline was 2.8× low. A send window >1 is the obvious candidate for restoring the parallelism deliberately rather than as a side effect of waste, so this matrix is what would either recover the ≤11% goodput risk or confirm it is real. Note this is *not* a licence to raise `DefaultSendWindowSize` — that remains a separate, independently-gated decision (see the item below); this is a measurement task. Prerequisite: a host reproducing the documented warm baseline, since that is precisely what M7's numbers lacked.
- **Formal release automation (tag-triggered `release.yml`, checksums/signatures, GitHub Release notes) has not been started** — this was the *original* scope of M5 in [[castr-project-plan]]; this session redirected the M5 slot to the showcase-docs work instead (see above), so release automation is carried forward here rather than dropped silently. `ci.yml`'s existing `package` job (from [[m3-test-ci-hardening-summary]]) already produces unsigned per-RID zips on every `main` push, which is a running start but not a tagged/checksummed/signed release.
- **M6's `SenderSession.DefaultSendWindowSize` stays at 1 (a no-op) despite consistent round-2/round-3 data suggesting 2 is now a real win** — see [[m6-throughput-pipelining]] and `SenderSession.DefaultSendWindowSize`'s XML doc for the full trial-by-trial numbers across all three rounds, both independently confirmed by a second reviewer in round 3. The default was deliberately kept at 1 anyway: all validation to date is single-machine/loopback-only, and round 1's initial default was already wrong once under exactly this kind of self-run benchmark. **Next step for whoever picks this up**: independent, cross-machine, real-LAN validation (ideally by someone who didn't write the round-2/3 benchmarks) is the stated bar for bumping the shipped default to 2 — `castr send --send-window-size 2` is available today for anyone who wants to opt in ahead of that.
- **A shared-gate fix for M6's independent-per-loop window double-counting was prototyped and reverted** (from M6 round 2, re-confirmed as an acceptable tradeoff in round 3) — `RunChunkCarouselAsync` and `HandleChunkRequestAsync` each independently cap concurrency at `sendWindowSize`, so simultaneous heavy repair traffic during an in-flight carousel can transiently push real concurrent sends to 2x the configured window (4, at a hypothetical default of 2); a shared `SemaphoreSlim` gate across both loops closes this but cost a measured, reproducible ~30% real-socket throughput regression even with zero actual contention in the benchmark — not worth it for a narrow, non-blocking correctness gap that's currently capped at a validated-safe spike of 2 (since the default is 1). Documented in-code (`SenderSession._sendWindowSize`'s field comment) with an explicit note that this gap must be re-validated before the default, or any recommended `--send-window-size` value, is raised above 1.
- **`Castr.Core.Discovery`'s mobile swarm-pull transport (`UdpUnicastTransport`/TCP framing) was not touched by M6** — it plausibly has the same synchronous receive-then-process architectural shape the multicast tier had before M6's fix, per systems-design's round-3 review, but this wasn't independently verified and no fix was attempted; carried forward as a real, likely-latent gap for a future pass.
- **`SwarmServeListener` accumulates connection-handler tasks without pruning** (new, from M4 QA — see [[m4-mobile-summary]]) — `RunAsync` adds every accepted connection's handler `Task` to a list and only awaits/prunes at cancellation, so a serve loop living across many mobile pull/reconnect cycles slowly leaks completed-but-retained `Task` references, with no cap on concurrent handlers either. Not a crash, not security-relevant (each handler's own allocations stay bounded) — worth fixing before a sender is expected to serve many mobile pullers over a long-lived session.
- **iOS `MobileReceiveViewModel` can't cancel an in-flight pull and doesn't cancel its `CancellationTokenSource` on dispose** (new, from M4 QA — see [[m4-mobile-summary]]) — unlike the Android `SwarmReceiveViewModel`, which has a working `CancelPull` command. UX/robustness gap only; a benign teardown race disposing the session's pull-gate semaphore mid-pull exists on both but is swallowed by each `PullAsync`'s catch-all.
- **iOS Simulator build blocked by an upstream libsodium packaging gap** (new, from M4 — see [[m4-mobile-summary]]) — `NSec.Cryptography` pins `libsodium` to `[1.0.22, 1.0.23)`, and that version ships no `iossimulator-arm64` (or any iOS Simulator RID) native slice at all, confirmed by inspecting the NuGet package's `runtimes/` folder directly. `ld` correctly refuses to link the device-platform `libsodium.a` into a simulator binary. `Castr.Core.Discovery`'s net10.0-ios head (the real NWBrowser/NWListener bindings) compiles and links fine on real Xcode CI — only the full `Castr.Gui.iOS` app-level link is blocked. Not fixable in Castr's code; needs either an NSec/libsodium release with simulator slices, or moving this CI leg to a signed real-device build (reintroducing the code-signing complexity deliberately deferred to M5). `ci-mobile-ios.yml` documents this inline and tolerates it via `continue-on-error` so CI still gates on what's real.
- **`SwarmReceiveViewModel` and `MobileReceiveViewModel` are two independently-designed implementations of the same mobile-receive concept** (new, from M4 — see [[m4-mobile-summary]]) — the Android head (`Castr.Gui.Android`) uses `SwarmReceiveViewModel`, the iOS head (`Castr.Gui.iOS`) uses `MobileReceiveViewModel`; both live in the shared `Castr.Gui` library, both compile, both are covered by their own test file, and both work, but they diverged during two independent build efforts that never converged on one shared design. Worth unifying deliberately in a future pass rather than as a rushed CI fix — see [[m4-mobile-summary]] for the QA assessment of real risk/divergence between them.
- **Hands-on mobile discovery interop still not device/emulator-validated** (deferred from [[adr-0002-mobile-discovery]], narrowed by M4) — the *code* (`NsdManager`/`NWBrowser` bindings) is now implemented **and CI-verified to actually compile/link against each platform's real SDK/Xcode toolchain** (M4), which is a real step up from the M0-era spike, but genuine Android↔iOS mDNS interop over a real LAN, on a real device or emulator, still hasn't been exercised — no device/emulator or macOS host exists on the dev machine. **The Android 13+ runtime-permission question is resolved (M4 research):** `NsdManager` service discovery does **not** require `NEARBY_WIFI_DEVICES` — that permission gates only Wi-Fi Direct/Aware/RTT/LocalOnlyHotspot, not NSD. NSD needs only the install-time `INTERNET` + `ACCESS_WIFI_STATE` + `ACCESS_NETWORK_STATE`, and `Castr.Gui.Android`'s manifest is wired accordingly. (Forward-looking: Android 17+ `ACCESS_LOCAL_NETWORK` will gate `.local` resolution, but NsdManager is exempt during the Android 16 opt-in.)
- **Repeating carousel rounds and NACK-suppressed unicast-targeted repair** (new, from M1 — documented scope trims in [[m1-core-summary]]) — both are natural low-risk extensions to `SenderSession`/`ReceiverSession`, not required for correctness, worth revisiting opportunistically rather than blocking on them now.
- **Manifest metadata (file names/sizes) remains unencrypted** now that M1.5 has landed — [[adr-0003-payload-encryption]] only encrypts chunk payloads by design. Revisit if a deployment needs filename/metadata confidentiality too; documented as a known, deliberate scope boundary, not an oversight.
- **Sender-side receiver allowlist** is a plausible future hardening noted in [[adr-0003-payload-encryption]] but not required now — currently any receiver that completes `JOIN_REQUEST` (i.e. already trusts the sender's signature) gets the content key, matching today's receiver-side-only authorization model.
- **Session-ID uniqueness is a caller contract, not yet enforced in `Castr.Core`** (new, from M1.5 QA — see [[m1.5-encryption-summary]]) — `ContentKeyWrap`'s HKDF salt is the transfer's session ID, so a caller reusing one across transfers between the same sender/receiver pair risks wrap-key reuse. `Castr.Cli` generates a fresh random session ID per invocation, so this is currently safe in practice, but `Castr.Core` still doesn't enforce it itself.
- **`Castr.Tui`'s heatmap/throughput views are approximations** (new, from M2 — see [[m2-ui-summary]]) — `TransferProgress` only exposes aggregate counts, not the raw `ChunkBitmap` or per-peer byte counts, so the live dashboard shows completion density and aggregate MB/s rather than exact per-chunk/per-peer detail. Revisit if a richer Core progress API is ever justified.
- **`Castr.Gui.Tests` is the only xUnit v3 project in the repo** (new, from M2 — see [[m2-ui-summary]]) — required by `Avalonia.Headless.XUnit`; QA confirmed it coexists cleanly with the rest of the solution's xUnit v2 projects (no double-counting, no version conflicts) but it's worth knowing about before adding more test infra.
- **Replay protection's `issued-at` freshness window is not implemented** (confirmed, from M3's security test pass — see [[wire-protocol]] and [[m3-test-ci-hardening-summary]]) — `ReceiverSession` never inspects it against the clock, and `ANNOUNCE` is currently ignored entirely. Low severity (trust is keyed to sender fingerprint, not session/timestamp) but a real, confirmed gap, not just a documentation lag — revisit if session-replay risk is ever reassessed.
- **Large/multi-file manifests are more loss-fragile since M3's packetization** (see [[m3-test-ci-hardening-summary]]) — a manifest between ~1200 bytes and 64 KB now splits across multiple `PacketFragment` datagrams; losing any one still stalls a receiver, since there's no manifest re-request path (the same M1 scope trim as above, now amplified from one losable datagram to N). Natural candidate to fix alongside the repeating-carousel/NACK-suppression item above.
- **Reassembly eviction is FIFO-by-creation, not true LRU** despite the docstring (`PacketReassembler`/`ChunkPacketAssembler`, found during M3 QA) — never triggered in any shipped scenario and correctness holds even if it did, but the naming should be corrected if this code is touched again.
- **E2E `tc netem` loss concentrates on each chunk's packet-0** (found during M3's combined QA — see [[m3-test-ci-hardening-summary]]) — the loss filter's matched datagram-size window only reliably catches the Merkle-proof-carrying first packet of each chunk, not every packet; the test still validly exercises repair (losing packet 0 strands the chunk) but at a lower effective rate than the nominal percentage. Worth tightening if E2E loss-rate precision ever matters.

## Where this fits

- [[castr-project]]
- [[wire-protocol]]
- [[repair-protocol]]
- [[security-model]]
- [[tech-stack]]
- [[adr-0001-ed25519-library]]
- [[adr-0002-mobile-discovery]]
- [[adr-0003-payload-encryption]]
- [[m1-core-summary]]
- [[m1.5-encryption-summary]]
- [[m2-ui-summary]]
- [[m3-test-ci-hardening-summary]]
- [[m4-mobile-summary]]
- [[m6-throughput-pipelining]]
